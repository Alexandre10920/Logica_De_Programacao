programa {
  funcao real areaRetangulo(real base, real altura) {
    retorne base * altura
  }
  funcao inicio() {
    real area = areaRetangulo(5.0, 5.0)
    
    escreva("A área do retângulo é: ", area, "cm²")
  }
}
